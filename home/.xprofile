#!/bin/sh

# -- Use gnome-disks --
# sudo mount /dev/sdb1 /home/joaopedro/HardDrive
# sudo modprobe r8169

xmodmap $XDG_DOTFILES_DIR/.config/keyboard/.Xmodmap_dead_greek

nitrogen --restore &
picom --backend glx --xrender-sync-fence &
flameshot &
dunst &
redshift &
discord --start-minimized &
nvidia-settings --load-config-only

# exec xmonad
# exec i3
